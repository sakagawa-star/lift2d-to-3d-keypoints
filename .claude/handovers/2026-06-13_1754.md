# 引き継ぎノート 2026-06-13 17:54

## 今回やったこと

- **feat-013 着手 → 実装 → 手動テストで問題噴出 → 最終的に中止**。3DGSレンダリング画像への3Dキーポイント重ね描き案件
  - 壁打ちで3入力（LCC製PLY / Pose2Sim製キャリブTOML / Pose2Sim製キーポイントC3D）と座標系を調査
  - requirements/design 作成 → Subagentレビュー（高・中ゼロ）→ 実装（`render_keypoints.py` + `test_feat013_*.py`、76テスト全成功）
  - 手動テストでキーポイントOK・3DGSレンダリングNG（壁）→ 原因切り分けで迷走
- **原因の確定（重要）**:
  1. ユーザーが渡していた `point_cloud.ply` が間違いデータだった（後で正しいものに置換）
  2. 黒い靄の主因は **`near_plane=0.01` によるカメラ至近1〜5cmの floater 描画**。`near_plane=0.5` でクリップすると病室がGTと一致して鮮明に映る（実機確認）。歪み（UT経路）は副次要因
  3. オクルージョン（前後関係の隠蔽）が要求仕様に入っていなかった（ヒアリング漏れ）
- **検証で確立した事実**（実機・数値）:
  - viewmat=`[[R,t],[0,0,0,1]]`（TOML直読み、R=Rodrigues(rotation)）が正しい。キーポイントがGT病室のベッド上に正確に乗ることで確認
  - ピンホール古典経路は `render.py` の `render_frame` と同一。歪みをgsplatのUT経路に乗せると靄・品質劣化
  - C3D→キャリブ座標 = `(pz,px,py)×0.001`（5カメラ再投影で検証）
  - オクルージョンは技術的に可能: gsplat `render_mode="RGB+ED"` で z深度マップ取得可。ただし near_plane で floater 除去が前提（near_plane=0.01: depth median 0.028m → near_plane=0.5: 1.944m）
- **ユーザー指示で feat-013/014 中止、feat-015/016 を新規作成**:
  - feat-015「ピンホール3DGSレンダリング（PNG出力、GT比較）」: docs完成（README/requirements/design）
  - feat-016「キーポイントのオクルージョン」: README枠のみ（requirements/designは feat-015完了後）
- BACKLOG更新、feat-013/014を中止ステータスに（フォルダは経緯として残す）

## 決定事項

- **機能を段階分割**: feat-015（3DGSレンダリングのみ）→ feat-016（キーポイント＋オクルージョン）。一度に作らない
- **feat-015 のスクリプト**: 既存 `render_keypoints.py` を3DGSレンダリングのみに作り直す（縮小）。feat-016 で機能追加
- **歪みは廃止しピンホール統一**（歪みをUT経路に乗せると靄）
- **near_plane はオプション化**（`--near-plane`、デフォルト0.1）。floater除去用
- **feat-015 design は feat-014 のレビュー済み内容（Subagentレビュー2回・高/中ゼロ）を踏襲**。`render_image` は device/dtype（CUDA・float32）明示の古典経路、`test_feat015_render.py` 新規、`test_feat013_render_keypoints.py` 削除（削除関数をimportしておりcollect失敗するため）

## 捨てた選択肢と理由

- **feat-014 として独立案件にする案**: feat-013 が完成していないのに別番号を立てたのは不適切とユーザー判断。feat-013/014中止、feat-015/016で作り直しに
- **feat-013 を縮小して段階化する案**: ユーザーが新番号 feat-015/016 を指示
- **歪みあり（gsplat UT経路）でレンダリング**: 黒い靄・品質劣化。標準3DGSはピンホール専用
- **near_planeを上げすぎる**: 手前の障害物（オクルージョン対象）もクリップされる。floater(〜5cm)を切り障害物(数十cm〜)を残す0.05〜0.1が妥当
- **PLYのfloaterを「間違いPLYだから」と片付けた**: 確証バイアス。正しいPLYでも near_plane調整で解決する問題だった

## ハマりどころ

- **確証バイアスで大きく迷走（ユーザーから2回指摘・性能を問題視された）**:
  1. 「データを探せとは言っていない、調査方法を探せ」— 「調査せよ」を勝手にデータ探索に拡大解釈してSubagent起動→中断
  2. ユーザーが `point_cloud.ply` を正しいものに置換したのに「間違いPLY」と思い込み続け、depth median 0.028m の異常を「間違いPLYだから」で片付け、near_plane の影響を検証しなかった。`ls -la` でタイムスタンプ（14:26更新、点数・bbox別物）を見れば置換済みと分かった
- gsplat の expected depth は半透明加重平均で、カメラ至近floaterに汚染される（near_planeで対処）
- Pose2Sim_Blender フォークの座標変換修正は非デフォルトブランチ `fix/coordinate-transform-rotation-bug` にある（main比較だと見落とす）

## 学び

- メモリに記録した教訓（`~/.claude/projects/.../memory/`）:
  - `feedback_investigate_method_not_data` — 「調査せよ」は方法の調査。勝手にデータ探索・実験実行しない
  - `feedback_visualization_needs_occlusion_check` — 3Dを画像に重ねる要求ではオクルージョンの要否を必ずヒアリング
  - `feedback_verify_facts_not_assumptions` — ユーザーの状態変更は事実確認（タイムスタンプ等）してから。異常値を思い込みで片付けない
- 大きな機能を一度に作ると原因切り分けが難航する。CLAUDE.md「シンプルな機能を一つずつ」を厳守すべき

## 次にやること

1. **feat-015 の実装**（ユーザーの docs 確認・実装承認待ち）:
   - `phase4/render_keypoints.py` を3DGSレンダリングのみに作り直す（design.md 通り。`render_image` の device/dtype明示に注意）
   - `tests/test_feat015_render.py` 新規作成（`camera_to_viewmat`, `select_camera`合成データ, 実データskipif）
   - `tests/test_feat013_render_keypoints.py` 削除（削除関数importでcollect失敗するため）
   - テスト実行（Subagent）→ `tests/results/feat-015_test_result.txt`
   - 手動テスト: `--near-plane 0.5` でGT(`0001_color.png`)と病室一致を確認
2. **feat-016 の docs作成・実装**（feat-015完了後）: キーポイント投影＋深度オクルージョン
3. **注意**: 未pushコメント（前回からの持ち越し `2de499e`, `7a6bcea`）+ 今回のdoc変更。push要否はユーザー確認

## 関連ファイル

- `docs/issues/feat-015-3dgs-render/` — README/requirements/design（実装対象）
- `docs/issues/feat-016-keypoint-occlusion/README.md` — 枠のみ
- `docs/issues/feat-013-keypoint-overlay-render/` — 中止。investigation.md に座標系・near_plane・確証バイアスの調査記録
- `docs/issues/feat-014-3dgs-render/` — 中止（feat-015の元）
- `docs/BACKLOG.md` — feat-013/014中止、feat-015/016追加
- `phase4/render_keypoints.py` — 現状は旧feat-013実装（歪みあり・キーポイント・オクルージョンなし）。feat-015で縮小予定
- `phase4/data/Blender/` — point_cloud.ply（正しい版、14:26）, Config_scene.toml, 0001_color.png(GT), *.c3d
- `~/.claude/projects/-home-sakagawa-git-lift2d-to-3d-keypoints/memory/` — feedback系メモリ3件追加
