# feat-010: 2D座標を静止画上にプロットして可視化

## 概要

`points_2d.csv` の入力ミスを視覚的に確認するため、指定したカメラの2D座標を元静止画上にプロットして画像ファイルとして保存する新規スクリプトを追加する。

## ステータス

Closed

## 背景

2Dの基準点座標を手作業で入力する際にミスが発生しやすく、現状は数値ベースでしか確認手段がない。元画像に点を重ねて表示することで、ずれや誤入力を一目で判別できるようにする。
