# codex-01: 初回レビュー結果

- 日付: 2026-08-31
- 対象: `docs/issues/update-003-adopt-herdr-review/README.md`、`docs/issues/update-003-adopt-herdr-review/design.md`
- ストリーム名: `rev-lift2d-update-003`（Herdr 対話方式。本案件がブートストラップのため AGENTS.md は未反映であり、定型指示は依頼文に記載。「[AGENTS.md適用]」マーカー確認は本案件では非適用）
- フェーズ: 反復（初回。`/new` なし）
- 指摘数: 高1 / 中1 / 低0
- トークン実測: total 236,182（input 233,035〔うち cached 182,784〕、output 3,147）
- rollout jsonl: `~/.codex/sessions/2026/08/31/rollout-2026-08-31T09-08-16-01a05525-33b0-77c2-84cf-d1f2a4aef06a.jsonl`

## Codex の指摘（要旨）

### 高: D2 が自己完結していない

design.md:14 は `docs/HERDR_SETUP.md` をテンプレートから cp するよう指示するだけで、179行の本文を埋め込んでいない。冒頭の「テンプレートを再参照しなくても作業できる」と矛盾し、指定テンプレートが利用できない環境では反映不能。

修正案: D2 に全文を埋め込む。少なくとも、自己完結性を主張せずテンプレート参照を必須前提として明記する。

### 中: 反映後検証が設計自身と矛盾し、必ず失敗する

D3-4 の置換後本文には「過程ログ（旧 full.log）は保存しない」という記述が残る（design.md:154）一方、検証手順は CLAUDE.md / .gitignore の full.log ヒットがゼロであることを要求している（design.md:254）。

修正案: 検証を「旧方式の実行手順・保存ルールとしての full.log が残っていないこと」に変更するか、D3-4 の説明を full.log を使わない表現へ改める。

### 致命的でない確認結果（指摘なし）

- .gitignore の専用ルール削除後も `*.log` により既存 full.log の ignore 挙動は維持。追跡済み `*.full.log` は存在しない
- 現行 CLAUDE.md の旧バッチ方式の詳細は全面移行に伴う意図的な置換であり、bwrap 対策文書への参照も新節に残る
- BACKLOG の対象行、feat-013/014 の正規化、表分断箇所は現物と一致

## Claude Code の対応方針

1. **高（D2）**: 「テンプレート参照を必須前提として明記する」案を採用する。転記元はローカルに実在するテンプレートリポジトリ（パス・コミット `19e4977` で固定。README.md「反映元」で実在確認済み）であり、/clear 後もディスク上に残るため反映作業は再現可能。design.md 冒頭の自己完結性の主張を「D2 のみ転記元をパス＋コミットで固定した参照方式」と正確化し、同一性検証用に転記元の sha256 を D2 と検証手順に追記する
2. **中（検証手順）**: 検証手順を「`grep -n "full.log" CLAUDE.md` のヒットが D3-4 で導入する『過程ログ（旧 full.log）は保存しない』の1箇所のみ」「`grep -n "full.log" .gitignore` がヒットしない」に修正する

→ design.md を修正のうえ、同一会話で再レビュー（解消確認）を依頼する（codex-02）。
