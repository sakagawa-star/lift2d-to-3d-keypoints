# 案1: 完全合成によるパイプライン動作検証 — 実験ログ

- 開始日: 2026-07-31
- ステータス: セットアップ承認待ち（Stage 2 未実行）

## 背景: 何が分かっていないか（未知X）

**初期ポーズ（位置・角度）を真のポーズからどれだけ離して置いても、パイプライン全体（レンダ→マッチング→3D化→PnP→反復）を回した後に推定値が真値に収束するか——この範囲（収束域）は分かっていない。**

- pipeline.md §4.2(b-1) の「数cm・数度」は、一つの故障モード（変位ゲート）だけから、未実測の仮定値（部屋の寸法・格子ピッチ・ゲート幅）で机上逆算した推測値であり、実証されていない
- M1（実験B）はこの未実証の閾値を判定基準に使い、かつ合格ライン（試行の何%が収まれば合格か）を事前定義しないまま「条件付きGo」と述べた——判定として不成立（M1 の位置づけは「差の分布の実測」に留まる。`../m1/report.md` の補遺参照）
- 収束域はマッチャーの視点差耐性・誤対応の挙動・反復の縮小性の合成で決まり、式だけからは出ない。実測が必要

## 未知Xの解き方（検討した案）

| 案 | 内容 | 長所 | 短所 |
|---|---|---|---|
| **案1（採用・まずこれ）** | 3DGSからレンダした合成画像をクエリ（実写の代わり）にする。真ポーズは自分で決めるので厳密に既知 | 真値が完全・何度でも回せる | ドメインギャップゼロ→出る収束域は楽観側上界 |
| 案2 | 代理環境の実写＋推定済みポーズを参照に初期値を振る | ドメインギャップ込み | 参照ポーズが真値でない |
| 案3 | 対象環境で自己整合性のみ測る | 対象環境そのもの | 「同一解に集まる」≠「真値に集まる」 |

ユーザー決定（2026-07-31）: **まず案1で、想定しているパイプラインが正常に動くかを検証する**。

## 実験手順の原則（ユーザー指示）

- **予測は各ステップの直前に立てる**（前段の結果によってステップの内容を変えるため、全段の予測を事前一括では作らない）
- 各段を「予測（直前に記載）→実行→照合→判断」のサイクルで進め、乖離があればその段を疑う。本ログに段ごとに記録する
- パイプラインを段に分け式を明示している目的は、入力から出力を予測し、予測と異なった場合にどの段が壊れているかを特定できるようにするため

## セットアップ（案・承認待ち）

| 項目 | 値 |
|---|---|
| シーン PLY | `/home/sakagawa/data/PLY/HandaiHospital-20260418-01/point_cloud/iteration_100/point_cloud.ply`（133MB、204万ガウシアン、標準3DGS形式・SHはDC成分のみ、PortalCam出力） |
| 参照TOML | `/home/sakagawa/git/pose2sim/Pose2Sim/20260418_osaka-hosp1/calibration/Config_scene.toml`（5カメラ、K≈1218、5係数、外部パラメータ入り。推定値であり真値ではないが近い値——ユーザー談） |
| 真ポーズ | cam05520126 の TOML 値を「真のポーズ」として採用（案）。案1では自分で決めた値が定義上の真値になるため、TOML の推定誤差は実験の正しさに影響しない（現実的な構図を借りるだけ） |
| K・δ | 同カメラの TOML 値（5係数） |
| クエリ画像 | 真ポーズから 3DGUT レンダ2回（色: `render_image(distort=True)`、深度+α: `render_depth_alpha_distorted`＝z_i を色として流しαで正規化）で生成（1920×1080）。※当初案の `render_image(distort=True, return_depth=True)` は gsplat 3DGUT 経路が RGB+ED 非対応のため実行不能と判明（下記実行記録参照） |
| 環境 | phase4 uv 環境（gsplat、`TORCH_CUDA_ARCH_LIST="9.0+PTX"` 必須） |
| PLY 互換性 | phase4 `render.py load_ply` の要求属性（x,y,z,f_dc_*,opacity,scale_*,rot_*）と一致することをヘッダで確認済み |

## Stage 2（疎通確認）— 実行前予測（記載済み・実行はセットアップ承認後）

**内容**: 真ポーズでレンダリングしクエリを生成。同じ真ポーズでもう一度レンダリングして比較する。

**予測**:
1. 同一ポーズ・同一入力の2回のレンダは画素一致する（差分最大絶対値 0、CUDA非決定性があっても ≤1/255 程度）。崩れたらレンダの非決定性の問題であり、以降の全比較の前提が崩れるため最初に確認する
2. レンダ画像は病室シーンとして成立した見えになる。崩れていたら座標規約か near_plane を疑う
3. αマップの大部分（>90%）が高α。低α領域が広ければ、以降のゲート(iv)で落ちる母数として記録する

## 実行記録

### 2026-07-31 Stage 2 実行1回目 — 失敗（想定外事象）

- 事象: `render_image(distort=True, return_depth=True)` が gsplat カーネルのアサーション `channels == 3` で停止（`rasterize_to_pixels_from_world_3dgs_fwd`）
- 原因: gsplat 1.5.3 の 3DGUT 経路（with_ut/with_eval3d）は render_mode="RGB+ED" 非対応。既存実績は「深度あり・歪みなし」（feat-016）と「歪みあり・深度なし」（feat-024）のみで、併用は一度も実行されたことがなかった。pipeline.md の「併用は実装済み」という記述はコードの見た目からの誤断定だった（codex レビューも見逃し）
- 対処（方式A、ユーザー承認済み）: 3DGUT レンダ2回方式に変更。深度は z_i を色として流しαで正規化（`render_depth_alpha_distorted`）。実装は codex-08〜10 でレビューし高・中ゼロに収束（指摘反映: 有効画素ゼロ時の失敗報告・ドキュメント整合・EDセマンティクス注記・pipeline.md 後段の旧記述差し替え）。pipeline.md Stage 2 の実装記述も訂正済み

### 2026-07-31 Stage 2 実行2回目（方式A） — 成功。予測3件すべて成立

| 予測 | 実測 | 照合 |
|---|---|---|
| 1. 再現性: 2回レンダの差 ≤1/255 | 画素差 最大=**0**、深度差 最大=**0.0** m、NaN位置不一致=0画素、α差 最大=0.0 | **成立**（完全一致。決定的） |
| 2. 見えの成立 | `query_cam05520126.png` 目視: 病室シーンとして明瞭（ベッド・ブラインド・三脚・機材が判別可能）。破綻・大きな靄なし | **成立** |
| 3. α>0.5 が >90% | α>0.5: **100.00%**、α>0.9: 99.99%、α中央値 0.9999 | **成立**（視野全面をシーンが覆う） |

深度の概況（以降の段の予測材料）: 中央値 3.009 m、p5/p95 = 0.528 / 4.756 m、α>0.5 で非有限深度は 0 画素、NaN 0%。near_plane=0.5。

**判断**: 疎通 OK。レンダリング・深度生成の再現性は完全決定的で、以降の段の比較基盤として成立。Stage 3（摂動レンダとマッチング）へ進んでよい状態。成果物: `query_cam05520126.png` / `query_depth.npy` / `query_alpha.npy` / `stage2_result.txt`

ユーザー確認: Stage 2 OK（2026-07-31）。

### Stage 3（摂動レンダとマッチング）— 実行前予測（実行前に記載）

**内容**: 真ポーズに既知摂動（**位置 5cm・角度 2°**、方向は乱数・シード固定。M1 実測の「良好データでの5〜6点PnPの典型誤差」に合わせた値）を加えた初期ポーズでレンダし、クエリと SIFT マッチング。レンダ側画素を深度+初期ポーズで3D化→真ポーズで投影した「正解変位場」で全マッチを採点する。

**予測**（Stage 2 実測 f≈1218・深度中央値3.0m・p5=0.53m に基づく）:
1. 正解変位場: 回転2°→約43px、並進5cm→Z=3mで約20px・Z=0.53mで約115px。**変位の中央値はおよそ40〜80px、近距離物体で最大150px程度**（スクリプトが厳密値を出力し照合）
2. SIFT マッチ: 合成同士（ドメインギャップゼロ・視点差のみ）なので比率テスト後**数百〜千のオーダー**。正解場と2px以内で一致する「正しいマッチ」率は**7割以上**、正しいマッチの位置決め誤差は**中央値1〜2px**
3. 外れた場合の疑い先: 正しいマッチ率が大幅に低い→繰り返し模様（ブラインド等）のSIFT誤対応（Stage 4 ゲート必要性の実証データ）。系統的オフセット→Stage 5 の3D化式か座標系の誤り

### 2026-07-31 Stage 3 実行 — 成功。予測すべて成立

実装は `stage3_perturb_match.py`（codex-11〜12 レビューで高・中ゼロに収束後に実行。指摘反映: 深度のbilinear補間+不連続除外、マッチ0件時の正常記録）。

| 予測 | 実測 | 照合 |
|---|---|---|
| 1. 正解変位場 中央値40〜80px、最大150px程度 | 中央値 **55.4px**、p5=47.2、p95=120.4、最大 **166.7px**（格子5023点） | **成立**（最大のみ見積り+11%、近距離p5深度の点。許容範囲） |
| 2. マッチ数 数百〜千のオーダー | 比率テスト後 **1370**（特徴点 約3100/画像） | **成立** |
| 2. 正しいマッチ率 7割以上 | **79.2%**（<2px、深度有効1315中1042） | **成立** |
| 2. 位置決め誤差 中央値1〜2px | 中央値 **0.24px**、p95 1.36px | **成立**（予測より良好。SIFTのサブピクセル精度） |

観察メモ:
- 誤マッチ（>5px）は15.7%存在する → Stage 4 ゲート/RANSACの除去対象。合成同士でもこの誤対応率であることは、実写での増加を見込む上での楽観側基準値
- 無効深度（シルエット・不連続）でのスキップは55/1370（4%）
- 成果物: `stage3_result.txt` / `render_perturbed.png` / `matches_vis.png` / `stage3_matches.npz`（Stage 4以降の入力）

**判断**: マッチング成立・oracle採点機構が機能。Stage 4〜6（ゲート→3D化→PnP→ポーズ誤差測定）へ進んでよい状態。

### Stage 4〜6（ゲート→3D化→PnP、反復1回分）— 実行前予測（実行前に記載）

**内容**: Stage 3 のマッチ集合（1370件）に Stage 4 ゲートを適用 → 生存対応を深度で3D化（Stage 5）→ PnP-RANSAC + 精密化（Stage 6）→ 推定ポーズと真ポーズの誤差を測定。反復1回で初期誤差（5cm/2°）がどこまで縮むかを見る。

**実験用パラメータ（このステップで確定した値）**:
- ゲート(i)（マッチャー信頼度）: SIFT は信頼度スコアを持たないため**比率テストで代替済みとして適用しない**（学習ベースマッチャー導入時に有効化）
- ゲート(ii): 5×5 窓の深度分散、τ_d = シーン中央値深度の2%（0.06m）
- ゲート(iii): τ_px^(0) = 250px（式 f·Δθ + f·B/Z_min = 1218×0.0349 + 1218×0.05/0.53 ≈ 157px に、回転・並進の幾何合成の余裕1.5倍。実測最大変位166.7pxを覆う）
- ゲート(iv): α > 0.5、有限深度、z ∈ [0.5, 10] m
- Stage 6: solvePnPRansac(閾値8px, ITERATIVE) + solvePnP精密化（既存実装と同型）。ロバスト非線形最小二乗（Huber/TRF）は本実験では省略（phase4 環境に scipy がないため。誤対応除去は RANSAC が担う。本実装時は pipeline.md 通り実装する）

**予測**（Stage 3 実測: マッチ1370・深度有効1315・正解率79.2%・位置決め誤差中央値0.24px に基づく）:
1. ゲート生存数 N: **900〜1300**（正しいマッチはゲートでほぼ落ちず、粗大変位の誤マッチの一部が(iii)で落ちる）
2. RANSAC インライア: **1000〜1200**、インライア純度（oracle正解率）**>95%**（8px閾値なので5〜8pxの準誤マッチが少数混入し得る）
3. 退化診断: 本実験では**診断値の記録のみ**（閾値 τ_vol/τ_z/τ_area はこの実験群の実測から較正する予定のため、fail 判定には使わない）。予測値: 点は画面全体に分布・深度レンジ数m・λ3/λ1 は共面でない値（>0.01 のオーダー）
4. **ポーズ誤差（本命）**: 誤差伝播式 σ_R ≈ (σ_m/f)√(C/N) に σ_m=0.5px（保守値）、N=1000、C=25 で回転 ≈0.004°、並進 ≈ Z·σ_R ≈ 0.2mm。実際は誤対応の残留・点分布の偏りで悪化するため、**回転 ≤ 0.05°、カメラ中心位置 ≤ 5mm** を成立予測とする（初期誤差 2°/5cm から**1桁以上の縮小**）。インライア再投影RMSE ≈ 0.3〜1px
5. 外れた場合の疑い先: ポーズ誤差が予測より1桁以上悪い→インライア純度を確認（周期構造の整合誤マッチの残留）または深度バイアス（Stage 5）。系統的オフセット→Stage 5 の式・座標系。退化診断不通過→点分布（診断値を確認）

### 2026-07-31 Stage 4〜6 実行 — 本命予測成立、副次予測2件に軽微な乖離

実装は `stage4_6_pnp_iteration.py`（codex-13〜14 レビューで高・中ゼロに収束後に実行。指摘反映: 分散計算を有効画素のみに、精密化 ok チェック、退化診断は記録のみと明記）。

| 予測 | 実測 | 照合 |
|---|---|---|
| 1. ゲート生存 N=900〜1300 | **N=1049**（(ii)1059・(iii)1348・(iv)1315 の AND） | **成立** |
| 2. インライア 1000〜1200 | **974** | ほぼ成立（下限を2.6%下回る） |
| 2. インライア純度 >95% | **91.4%** | **乖離（小）**: 8.6%が oracle 不正解（>2px）のままインライア入り |
| 3. 退化診断（記録のみ） | λ3/λ1=**0.269**、深度レンジ**4.20m**、凸包面積比**0.675** | 健全な点配置（予測整合。閾値較正の参考値として記録） |
| 4. RMSE 0.3〜1px | **1.561px** | **乖離（小）** |
| 4. **ポーズ誤差 回転≤0.05°・位置≤5mm** | **回転 0.0295°、位置 1.61mm**（初期 2°/5cm から **68x/31x 縮小**） | **成立（本命）** |

**乖離2件（純度・RMSE）の解釈**: RANSAC 閾値 8px（既存実装と同値）が、周期構造由来の 2〜8px の準誤マッチを排除できずインライアに混入させた。RMSE 1.56px はこの混入を反映した値。ポーズ誤差が予測内に収まったのは、準誤マッチの誤差が方向的に偏らず平均化されたため。**含意**: 反復 k≥1 では pipeline.md の段階的引き締め（τ_px・RANSAC閾値を絞る）が有効に働く見込みで、本実装ではロバスト損失（Huber）も乖離縮小に寄与するはず。この2件は故障ではなく、8px 閾値の限界の定量観測として記録する。

**判断**: 反復1回で初期誤差 5cm/2° → 1.6mm/0.03°。パイプラインの力学（Stage 2〜6 の一巡）は合成条件下で成立。次は Stage 7（反復2回目で収束確認）または摂動量を振った収束域マップへ。成果物: `stage4_6_result.txt` / `stage4_6_pose.npz`
