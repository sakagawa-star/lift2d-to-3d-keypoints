# M3 実験ログ（予測→実行→照合）

記録方式: 各ステップ直前に予測（数値）を立て、実行し、照合する。乖離が出たらその段を疑う。

## M3-0a: Config_scene の K・歪みでの n0=10 再サンプリング再計測（2026-07-31）

- スクリプト: `m3_0a_resample_p95.py`（codex-24〜25 レビュー収束後に実行）
- 目的: criteria.md §5.3 一致性閾値（p95）の数値確定

### 予測（実行前に記録）

Config_scene と intrinsics_all の焦点距離差は数十px（約5%）規模なので、分布の形は大きく変わらず、判定対象3カメラの p95 は M1 参考値（位置 7.5〜9.6cm・角度 0.98〜2.27°）の ±50% 以内に収まる。基準ポーズの inlier/outlier 名簿は多少入れ替わり得る。

### 実測（`m3_0a_results_summary.txt`、OpenCV 4.13.0 / NumPy 2.2.6）

| カメラ | 全点 | inlier | p95 位置差 | p95 角度差 |
|---|---|---|---|---|
| cam05520126 | 30 | 28 | 2.55 cm | 0.35° |
| cam05520129 | 22 | 21 | 2.29 cm | 0.46° |
| cam41520554 | 17 | 14 | 1.84 cm | 0.39° |
| cam41520556 | 22 | 22 | 2.05 cm | 0.44° |
| cam41520557 | 36 | 36 | 2.40 cm | 0.41° |

### 照合: 予測は大きく外れた（乖離の記録と解釈）

- p95 は M1 参考値の約 **1/4**（±50% 予測を大幅に下回る縮小）。さらに M1 で「劣化データ」だった cam41520554/557 が他と同水準になった
- 外れ値（8px 基準）の激減: cam41520557 は 16/36（44%）→ **0/36**、cam41520554 は 8/17（47%）→ 3/17（18%）
- **解釈（実測から言える範囲）**: M1 の高い外れ値率は **intrinsics_all 条件下の系統的再投影誤差を大きく含んでおり、手動点そのものの品質指標として使うのは誤りだった**。Config_scene 条件では不整合が大幅に解消する（0〜18%）。系統誤差の由来（Config_scene の K・歪みがこれらの手動点と整合的に推定されている可能性が高い）は状況証拠であり、Config_scene の生成過程の資料では確認していない
- 教訓: 外れ値率・分布は「どの K・歪みの下での値か」を明示しないと品質指標として比較できない（findings.md §1.8 の注記対象）
- **criteria への影響**:
  1. §5.3 の閾値を実測 p95 で確定（当初案より約4倍厳しい物差しになる。物差しの論理は不変: 「基準ポーズの自己ばらつきと不可分なら一致」）
  2. §3 の層別根拠（M1 外れ値率による低信頼カメラ除外）が崩れた。Config_scene の下では全5カメラの点データが整合的（外れ値率 0〜18%）なので、判定対象を5カメラに拡大する（**M3 本実験の実行前・lock 前の改訂**であり、結果を見た事後変更ではない）
  3. §5.4 の inlier 名簿は M1 版でなく M3-0a 版（Config_scene の下での名簿）を使う

### 再実行（2026-07-31、codex-26 指摘対応）

`REQUIRED_CAMS` を判定対象5カメラ（層別廃止後の criteria §3）に更新して M3-0a を再実行した。`m3_0a_results_summary.txt` は初回実行と**完全一致**（diff 差分ゼロ。決定的計算の確認を兼ねる）。§5.3 の確定値に変更なし。
