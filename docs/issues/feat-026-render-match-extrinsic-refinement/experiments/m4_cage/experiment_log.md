# M4 実験ログ（檻環境パイロット）

- 進め方: criteria.md（locked 2026-08-01）に基づき、各ステップ直前に予測（数値）を記録 → 実行 → 照合する
- 実験スクリプトは Codex レビュー収束後に実行する

## Stage 0: 疎通確認（criteria.md §8-1）

### 予測（実行前に記録。2026-08-01）

| 項目 | 予測 | 根拠 |
|---|---|---|
| 6点PnP | 成功。inlier 6/6（RANSAC 8px で脱落なし） | 手動点とチェスボード K は自己整合的（同じ点で解くため）。М3 の手動点中央値 2.5px は Config_scene K との差を含む値であり、ここでは K を揃えて解くのでそれより小さいはず |
| 手動6点の再投影誤差（初期ポーズ） | 中央値 < 2px | 同上。6点PnP は6点への当てはめなので残差は小さく出る（過適合気味に見える点に注意） |
| 重畳PNG の目視 | 檻の構造が概ね重なる（ずれは画素十〜数十px オーダー） | PLY は実寸整列済み（ユーザー確認）。M1 実測の6点PnP誤差 3〜5cm/1°未満 → 画像上で数十px 程度のずれに相当 |
| レンダ有効画素率（α>0.5） | > 0.8 | 檻内部を向く視点でシーンが視野を覆う想定。壁際外挿破綻（b-4）があれば下振れする |
| 深度 z 下限 0.5m との干渉 | 有効画素の < 5% | シーン bbox は ±4m 程度あり、カメラが檻に密着していなければ干渉は小さいはず。**檻固有の不確実性が大きい項目**（外れたら b-2/b-4 の先行信号） |
| SIFT キーポイント数比（レンダ/実写） | 0.4〜1.0 | M4 の PLY は SH0 のみで視点依存の質感が乏しく、実写より少なめに出ると予想。M3 での参照値はないため初計測 |

- 予測が外れた場合: 乖離した項目を檻固有の要因（(b-1)〜(b-4)）と切り分けて記録する。**criteria の定数・閾値は動かさない**（§4）

### 実測（2026-08-01）

- **6点PnP: 失敗**（solvePnPRansac が inlier を見つけられず）。Stage 0 は最初の段で明示的失敗（`m4_stage0_result.txt`）。レンダ以降は未実行
- 診断（スクラッチパッドで実施。criteria の定数は変更していない）:
  - RANSAC 閾値を 300px まで緩めてようやく解が出るが、再投影誤差は中央値 44px・最大 351px（基準_006）
  - leave-one-out: どの1点を除いても残り5点の中央値が 42〜98px → 単一点の取り違えでは説明できない
  - 2D 座標系仮説（Y反転/X反転/XY反転/XY入替）: いずれも「そのまま」より悪化 → 座標系の向き違いではない
  - 基準_005↔006 の対応交換: 不成立
  - **決定的な切り分け**: 固定構造（檻の四隅）の 基準_001〜004 の4点のみで PnP を解くと、**4点の残差は 1.58〜4.44px で整合**。その解で可動アクリル構造物（梯子）上の 基準_005 / 006 を再投影すると **143px / 378px**
  - 可視化: `m4_stage0_pnp_diag.png`（緑=手動2D点、赤=再投影、黄線=残差）

### 照合（2026-08-01）

- 予測「6点PnP 成功・inlier 6/6・再投影中央値 < 2px」は**外れ**
- 乖離の在り処はパイプラインでも criteria でもなく**入力データ**: 固定4点は K・2D・3D が互いに数px で整合しており（＝チェスボード K・手動プロット・3D測定の品質は良い）、基準_005 / 006 の3D座標だけが画像と 143〜378px（この距離では概ね 8〜20cm 相当）乖離している
- **原因確定（2026-08-01 ユーザー確認）: 基準_005 / 006 の3D座標のプロットミス**。ユーザーが `kijunten_locations.csv` を修正する（梯子の移動仮説は否定。シーン自体の不整合ではないため、レンダ・マッチング段への影響懸念も解消）
- 修正後に Stage 0 を再実行する（criteria・スクリプトは変更なし。予測は本節の値を引き継ぐ）

## Stage 0 再実行（2026-08-01。基準_005/006 の 2D・3D 修正後）

### 実測

| 項目 | 実測値 |
|---|---|
| 6点PnP | 成功。**inlier 5/6**（基準_002 が 19.8px で RANSAC 8px から脱落） |
| 手動6点の再投影誤差 | 中央値 2.23px（002 以外は 2.0〜5.4px、002 のみ 19.8px） |
| カメラ中心（世界座標） | (1.879, 0.744, 0.640) — 失敗前診断の固定4点解 (1.873, 0.740, 0.631) と ~1cm 一致 |
| レンダ有効画素率（α>0.5） | 1.000 |
| 有効深度分布 | min=0.501 / p5=0.676 / 中央値=1.287 / p95=1.934 / max=2.479 m |
| z範囲(0.5,10)m との干渉 | 下限以下 0.0% / 上限以上 0.0% |
| SIFT キーポイント数 | 実写 3710 / レンダ 3898、比 1.051 |
| 重畳PNG 目視（Claude 所見） | 実写とレンダの構造（止まり木・金網・アクリル壁・梯子）が僅かなゴーストで重なる。座標系整列は良好。ユーザー目視確認待ち |

### 照合

- 6点PnP 成功・再投影中央値: **概ね予測通り**（中央値 2.23px vs 予測 <2px）。ただし inlier は 6/6 予測に対し **5/6**——基準_002（画像右下隅、歪み最大の領域）が 19.8px で脱落。単発の2Dプロット誤差か K の隅の精度かは未特定（初期ポーズは残り5点で成立しており運用経路の想定内。b-4 の切り分け材料として記録）
- 有効画素率 1.000（予測 >0.8 ✓）、z干渉 0%（予測 <5% ✓）。ただし深度 min=0.501m は near_plane=0.5 とほぼ一致——0.5m より手前の内容は near_plane で切られるため干渉率 0% は「0.5m 未満が存在しない」証明ではない（p5=0.676m なので実害は小さい）
- SIFT 比 1.051 は予測 0.4〜1.0 の**上側に外れ**（良い方向）。SH0 のみの PLY でもキーポイント数は実写同等——「SH0 でマッチングが痩せる」懸念は Stage 0 段階では観測されず
- **檻の撮影距離は 0.5〜2.5m と M3（阪大病院）より大幅に近い**。3D 1cm 誤差が画像上 ~13〜27px に増幅されるスケールであることを本実験の解釈時に念頭に置く

## 本実験（m4_run.py。criteria.md §5〜§7）

- スクリプト: `m4_run.py`（codex-47〜49 で収束。M3 実装との同一性・criteria との一致を確認済み）

### 予測（実行前に記録。2026-08-01）

| 項目 | 予測 | 根拠 |
|---|---|---|
| §6.1 成立性 | 成立（3チェーンとも60サンプル収集、明示的失敗なし） | Stage 0 で SIFT 数比 1.051・有効画素率 1.000。初期ポーズは正対に近く M2 収束域の十分内側 |
| ratio後マッチ数 | 200〜600件 | M3 実写 258〜446件。檻は金網・構造物でテクスチャは多いが、ガラス反射と周期構造が不利に働く |
| §6.2 受理 | 単峰高合意（f_c ≥ 0.7、二峰なし）で受理 | M3 では5カメラ中4が f_c=1.000。二峰リスクは檻の周期構造次第（外れたら b-3 の証拠） |
| クラスタ RMS 半径 | 2〜10mm | M3 実写ジッタ床 1.8〜6.3mm。近接シーンでは3D誤差の画素感度が高い分、上振れし得る |
| §6.4 初期解→最終解の差 | 位置 ≤ 10cm・角度 ≤ 2°（前提条件 50cm/10° を大幅に満たす） | M1 良好データの6点PnP誤差 中央値 3〜5cm/1°未満。Stage 0 の手動点整合 2.23px は良好データ相当 |
| RMSE（k≥1） | 1.0〜2.0px | M3 実写 0.90〜1.24px。近接による3D誤差の画素増幅と周期誤マッチでやや上振れ予測 |
| (b-3) 準誤マッチ率 | 30〜50%（M3 上限側） | 檻の格子は M3 のブラインドより過酷な周期構造。cam05520129（37〜39%）超えもあり得る |
| (b-2) 深度除外（NaN/広がり） | M3 より高率 | 格子・細線はシルエット境界が多く、bilinear 2×2 広がり除外に引っかかりやすい |
| depth_range 警告 | 常時点灯（<1.4m） | シーン深度が 0.5〜2.5m しかない（Stage 0 実測 p5=0.676/p95=1.934m）。criteria §6.5 に記載済みのシーン依存性 |

- 予測が外れた場合: criteria の定数・閾値は動かさず、乖離を (b-1)〜(b-4) の切り分けとして記録する

### 実測（2026-08-01）

- **§7 判定: No-Go（成立性不成立）**。3チェーンすべて k=0 で「ゲート生存 N < 30」の明示的失敗（A: N=26 / B: N=14 / C: N=19）。silent failure ではない
- §6.3 実測（k=0、3反復）:
  - (b-1) 逐次通過: 生候補 3898〜4461 → **ratio後 97〜109** → 変位ゲート後 66〜78 → **深度系ゲート後 N=14〜26** → （RANSAC 未到達）
  - (b-2) 深度系除外（変位通過分母）: **NaN/2×2広がり除外 39.7〜54.5%**、**深度分散除外 20.5〜26.9%**、α除外 0%、z範囲除外 0%
  - (b-4) レンダ有効画素率 1.000、SIFT数比 1.051〜1.202 → レンダ品質は問題なし

### 照合（2026-08-01）

- 成立性の予測（成立）は**外れ**。外れの内訳が檻固有の2要因を定量特定した:
  1. **ratio後マッチ数が予測 200〜600 に対し実測 97〜109**（M3 実写 258〜446 の 1/3〜1/4）。生候補は 4300 件あるのに ratio test で 97.5% が落ちる——檻の格子・金網の周期構造で記述子が曖昧化し、最近傍と第2近傍の距離比が締まらない（(b-3) が誤マッチではなく**マッチ数の枯渇**として発現）
  2. **深度系ゲートが変位通過分の 60〜75% を除外**。NaN/広がり除外（40〜55%）は細線・金網のシルエット境界で 2×2 近傍の深度が跳ぶため、分散除外（20〜27%）は格子越しの背景で局所深度分散が大きいため。M2/M3（壁・床など連続面が支配的な病院シーン）で較正されたゲートが、細線構造だらけの檻では通過点をほぼ残さない（(b-2) の答え）
- 予測が当たった項目: 深度除外は M3 より高率（方向は的中、程度は予測以上）、レンダ品質良好（b-4 は問題なし＝「3DGS 再撮影」の分岐は根拠なし）、6点PnP・初期ポーズは良好
- **失敗モード = 「マッチ不足（ratio 律速）+ 深度不良（細線構造）」の複合**。首振り型の角度誤差ではないため Stage 1.5（回転粗整列）は適用対象外（レスキュー実行せず）
- criteria の定数・閾値は動かしていない。次の一手（roadmap の切り分けフロー: マッチャー変更 / ゲート定数の近接シーン再較正を別実験 M4-1 として criteria lock / 手法棄却）は**ユーザー判断待ち**

## M4-1 フェーズ0+1: 深度ゲート定数の較正スイープ（m4_1_sweep.py。criteria_m4_1.md §4）

### 予測（実行前に記録。2026-08-02）

| 項目 | 予測 | 根拠 |
|---|---|---|
| フェーズ0 再現 | 期待値表（criteria_m4_1 フェーズ0）と全列完全一致 | パイプラインは決定論的（レンダ・SIFT・摂動 seed 固定）。不一致なら実装バグ |
| 真NaN除外（緩和で救済不能分） | 各チェーン 0〜5件 | M4 実測 alpha_cov=1.000（α>0.5 が全画素）→ 深度 NaN 画素はほぼ存在しないはず。excl_depth_nan の大半は広がり超過と予測 |
| 最大緩和 (0.20, 0.08) での N | A: 40〜60 / B: 25〜45 / C: 30〜50 | シルエット跨ぎ点の広がり・分散は閾値を1〜2桁超える（前景~1m vs 背景~2m で相対広がり100%級）ため全量は戻らない。救済率を除外合算（A:52 B:52 C:54）の3〜6割と置いた |
| 適格組（全チェーン N≥30）の存在 | 存在する（ただし律速はチェーン B の N=14。変更度 2〜5 の組になる） | B は +16 必要 = 除外52件の約31%救済で到達。救済率予測 3〜6割の下限側でも届く |
| 選定される組 | (0.10〜0.20, 0.04〜0.08) のいずれか。変更度最小則により最初に適格になる組 | 現行 (0.05, 0.02) は M4 実測で不適格確定（N=26/14/19）のため変更度 ≥1 |

- 予測が外れた場合: criteria_m4_1 のグリッド・選定規則は動かさず、乖離を除外内訳（真NaN/広がり分離）の実測から切り分けて記録する。適格組なしならフェーズ1 で No-Go 終了（§5.5）

### 実測（2026-08-02）

- **フェーズ0: 成立**。3チェーンとも期待値表と全列完全一致（スイープ基盤は M4 と同一であることを確認）
- **フェーズ1: 適格組なし → No-Go（criteria_m4_1 §5.5。フェーズ2 に進まない）**
- 全12組×3チェーンの内訳: `m4_1_sweep_table.csv`。要点:
  - **真NaN除外 = 全チェーン 0 件**（除外は 100% 広がり超過。深度NaN画素は実質存在しない）
  - **広がり緩和は分散ゲートに丸ごと吸収される**: var=0.02 固定で spread を 5→20% に緩めても N は 26/14/19 から**1件も動かない**（例: チェーンA で excl_spread 31→8 に減った分、excl_var が 21→44 に増加。救済点が全量、分散ゲートで再除外される完全な冗長関係）
  - **分散緩和のみが N を動かすが飽和する**: var 2→8%（τ_d² で16倍）でも A: 26→39 / B: 14→24 / C: 19→26。最大緩和 (0.20, 0.08) でも B=24 < 30
  - 実測救済率: A 25% / B 19% / C 13%（変位通過分の除外に対する回復率）

### 照合（2026-08-02）

| 予測 | 実測 | 判定 |
|---|---|---|
| フェーズ0 完全一致 | 完全一致 | 的中 |
| 真NaN 0〜5件/チェーン | 0件（全チェーン） | 的中（除外の実体は広がり超過側） |
| 最大緩和での N: A 40〜60 / B 25〜45 / C 30〜50 | A 39 / B 24 / C 26 | **全チェーンで下限割れ**（救済率 13〜25% は予測 30〜60% を下回る） |
| 適格組が存在する | 存在しない | **外れ** |

- 外れの原因（内訳データから確定）: 除外点は「閾値をわずかに超えた較正ミスマッチ」ではなく、**広がり・分散の両方を同時に大きく超える真の深度不連続点**（細線・格子のシルエット跨ぎ）だった。広がりと分散は同じ点集合に冗長に発火しており、片方を緩めても他方が捕まえる。つまり M4 の要因②は「病院較正の定数が細線シーンに合っていない」のではなく、**「細線シーンではマッチ点の相当数が本当に深度リフト不能な場所に乗る」**が正しい解釈
- 含意: 深度ゲートの**定数**再較正（候補1）ではこのシーンの成立性は回復しない。回復経路は (a) マッチ母数を増やして除外率を数で吸収する（候補3: マッチャー変更）、(b) ゲートのアルゴリズム変更（有効画素のみの補間、不確実性重み付け等。候補1のスコープ外と criteria §1 に明記済み）、のいずれか
- criteria_m4_1 のグリッド・選定規則は動かしていない（§6: 事後追加・次点再実行の禁止を遵守）

---

# M4-2 実験ログ（予測 → 実測 → 照合）

criteria_m4_2.md（locked 2026-08-02）§8/§9 に基づく記録。各フェーズの予測は**実行直前**に記す。

## フェーズ0: 疎通

### 予測（2026-08-02、実行前に記録）

1. **SIFT 再現**: 6点PnP 成功 + 3チェーン k=0 の N が M4 実測（A=26 / B=14 / C=19）と**完全一致**すると予測する。根拠: 決定論パイプライン（M4-1 フェーズ0 で全列一致の前例）
2. **マッチャー疎通**: 3マッチャーともマッチ>0・check_meta 通過。生マッチ数の予測レンジ（チェーンA ペア、不確実性大と明記）: loftr_outdoor **1000〜8000**（病院 1280x720 実測 5752〜8463 に対し、檻の周期構造・近接シーンでの劣化幅が未知のため下限を大きく取る）/ loftr_indoor_new 100〜6000 / mast3r **1500〜2500**（病院実測 2138〜2274。グリッド上限 2304 支配のためシーン依存性が小さいと予測）
3. **再現性**: 3マッチャーとも決定的（M3b フェーズ0 実測より）

### 実測（2026-08-02）

1. SIFT 再現: 6点PnP 成功（inlier 5/6、outlier=基準_002。M4 と同一）、3チェーン k=0 の N=26/14/19 で**完全一致**（ratio後 97/109/108 も M4 実測レンジと一致）
2. マッチャー疎通: 全マッチャー OK。生マッチ数（チェーンA）: loftr_outdoor **3235** / loftr_indoor_new **1816** / mast3r **2299**。check_meta 全通過
3. 再現性: 3マッチャーとも決定的（座標最大差 0.0000px）

### 照合

- 予測3件すべて的中（生マッチ数は全レンジ内: 3235∈[1000,8000]、1816∈[100,6000]、2299∈[1500,2500]）
- 観測: 檻の loftr_outdoor は病院（1280x720 で 5752〜8463）の約半分に劣化するが、**SIFT の ratio 後（97〜109）の約30倍**を維持。mast3r は予測どおりシーン依存性が小さい（病院 2138〜2274 → 檻 2299）

## フェーズ1: マッチング段のオフライン比較（3チェーン × 9設定）

### 予測（2026-08-02、フェーズ0 の観測を根拠に実行直前に記録）

1. **生マッチ数（th=0.0、チェーン間レンジ）**: loftr_outdoor 2500〜4000 / indoor_new 1300〜2500 / mast3r 2200〜2400（チェーンB/C は摂動ポーズだが k=0 のレンダは近傍のため A と同程度と予測）
2. **変位ゲート通過率**: 60〜95%（M3b の P0 ではほぼ100%だったが、檻の摂動チェーン+周期構造での誤対応増を見込み下限を下げる）
3. **深度系除外率（変位通過分母。本実験の核心）**: **30〜60%** と予測する。根拠: M4 SIFT 実測は 60〜75% だったが、SIFT キーポイントはコーナー・エッジ（=細線・格子）に集中するのに対し、学習マッチャーの準密グリッドは面領域にも点を持つため、シルエット跨ぎ点の比率は下がると予測。ここが 60〜75% のままでも母数30倍なら N≥30 は満たす
4. **ゲート後 N**: loftr_outdoor@0.0 で **400〜2500**（3チェーンすべて N≥30 を満たす=適格、高確度）/ mast3r 600〜1800（適格）
5. **採用設定**: 病院と同じく th=0.0/0.2 が同数なら **loftr_outdoor@0.2**。檻で conf 分布が変わり 0.2 で減る場合は minN 最大の設定
6. **遷移**: LoFTR 適格 → フェーズ2（LoFTR E2E）へ

### 実測（2026-08-02。詳細: m4_2_stage_table.csv）

- 生マッチ（th=0.0）: loftr_outdoor A=3235 / B=2019 / C=1990、indoor_new 570〜1816、mast3r 2105〜2299
- 変位ゲート通過率: 98〜100%（全設定）
- **深度系除外率（変位通過分母）: loftr_outdoor 28〜32% / mast3r 24〜26%**（SIFT の M4 実測 60〜75% から大幅低下）
- ゲート後 N: loftr_outdoor@0.0/@0.2 で **A=2265 / B=1353 / C=1405**（SIFT の N=26/14/19 の**約100倍**）、mast3r 1580〜1741
- 適格: LoFTR 7設定（indoor_new@0.8 のみ N=1〜2 で不適格）+ MASt3R。**採用 loftr_outdoor@0.2**（0.0 と同数 → 閾値高い方。病院と同一パターン）

### 照合

- 的中: 予測4（N レンジと適格、高確度どおり）、予測5（採用 loftr_outdoor@0.2）、予測6（遷移: フェーズ2へ）
- 部分的中/外れ: 予測1（outdoor B/C 2019/1990 は下限 2500 割れ——摂動ポーズで生マッチが4割減る現象は未予測）、予測2（変位通過率 98〜100% は上限 95% 超え——檻でも誤対応はほぼ出なかった）、予測3（除外率 24〜32% は予測レンジ 30〜60% の下限近傍〜下振れ）
- **核心の解釈**: M4 の要因②「深度除外 60〜75%」は SIFT キーポイントの分布特性（細線・エッジ集中）との複合現象だった。学習マッチャーは母数を増やすだけでなく**除外率自体も半分以下に下げる**。母数×通過率の複合効果で N は SIFT 比 約100倍

## フェーズ2: LoFTR E2E（loftr_outdoor@0.2、M4 手順の完全再実行）

### 予測（2026-08-02、実行直前に記録）

1. **成立性（§6.1）**: 3チェーンすべて成立（k=0 N≥30 + 60サンプル収集。N は桁で余裕）。高確度
2. **受理形態（§6.2）**: **単峰高合意**（f_c ≥ 0.7、第2 < 0.2）と予測。根拠: M3b で LoFTR の系列間ばらつきは 0.28〜0.51cm と小さい。リスクは檻の周期構造による1格子ずれの偽アトラクタ（発生すれば二峰→仲裁）だが、フェーズ1 の変位ゲート通過率 98%+ から大変位の誤対応は少なく、主予測は単峰
3. **前提条件（§6.4）**: 初期解→最終解の差 ≤ 50cm/10° を**満足**（M1 実測の6点PnP 初期誤差は良好データで 3〜5cm/1° 未満。リファインの移動量は数cm 想定）
4. 診断値: RMSE 1〜3px、準誤マッチ率は M3 参照値（18〜40%）と同程度以下
5. **総合: M4-2 は LoFTR で Go**（フェーズ3 は実施されない）と予測

### 実測（2026-08-02。詳細: m4_2_result.txt / m4_2_history_loftr.npz）

- **§6.1 成立性: 成立**（3チェーン成立、60/60 サンプル収集、silent failure なし）
- **§5-3 pooled 合意: f_c=1.000（60/60）、第2クラスタ 0.000、RMS半径 1.49mm/0.0617°** → **§6.2 受理（単峰高合意）**
- **§6.4 前提条件: 満足**（初期解→最終解 1.43cm/1.414° ≤ 50cm/10°）
- **判定: Go（成立性○ 受理○ 前提条件○）→ M4-2 全体 Go。フェーズ3（MASt3R）は遷移表どおり実施せず**
- 診断・記録事項（合否に使わない）:
  - k≥1 の RMSE 1.187〜1.268px（M3 参照値と同水準。警告なし）
  - 準誤マッチ率 37.8〜49.1%（中央値 41.4%。M3 参照上限 40% をやや超過——檻の周期構造の反映と解釈、記録のみ）
  - 手動6点再投影中央値: 初期解 2.233px → 最終解 4.545px に**悪化（記録のみ）**。手動点は可動構造物上の点の修正履歴があり（M4 Stage 0）、基準の絶対精度自体に限界があるため切り分け材料として記録
  - §6.4 の角度差 1.414° は M1 良好データの目安（1°未満）よりやや大きい（記録のみ）
  - k≥1 では生マッチが 4112〜5158 に増加（整列が進むとマッチャーがさらに対応を見つける、M3b と同傾向）

### 照合

- **予測1〜3・5 すべて的中**: 成立性○、受理形態は主予測どおり**単峰高合意**（二峰の偽アトラクタは発生せず。f_c=1.000 は予測以上の合意度）、前提条件○、総合 **Go**
- 予測4は部分的中: RMSE は k≥1 で 1.2px 前後（レンジ内）だが、準誤マッチ率は上限想定（40%）をやや超える 41〜49%
- 檻問題の総括: M4 No-Go の2要因（①ratio 律速の枯渇、②深度除外 60〜75%）は、マッチャー変更（候補3）single change で**同時に解消**された（①母数 20〜30倍、②除外率も 28〜32% に半減）。criteria lock 下の判定として、**檻シーンの外部パラメータ精緻化は loftr_outdoor@0.2 で成立**
