**高:** なし。  
**中:** なし。  
**低:** なし。

D3 節は、実行前基準として致命的な穴は見当たりません。

`1.0px` 閾値の根拠は妥当です。A/B の 1.77cm 差は `f≈1217`, 距離 2〜3m なら再投影差 7〜10px 級になり得る一方、判定に使う名簿14点の中央値ゆらぎを `≲1px` と見て、1.0px 以上だけを有意差扱いにする設計は保守的です。

仲裁不能時も `差 < 1.0px` で明示的に保留するため、無理に A/B を選ぶ設計にはなっていません。測定定義も `criteria.md §5.4` と同一、判定対象は名簿中央値、全点中央値は記録という形で固定されており、実験後に合否を動かせる曖昧さは見当たりません。
---

## Claude Code の記録（2026-07-31）

- 対象: D3 判定基準（experiment_log.md）レビュー / session id: 019fb6c9-967d-7020-b17f-c648607c8892
- 高・中・低ゼロで一発収束。仲裁スクリプト d3_arbitrate.py を作成し codex-40 でレビューする
