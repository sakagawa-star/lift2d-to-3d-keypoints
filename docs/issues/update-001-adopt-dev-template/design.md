# update-001 反映設計書: 開発ドキュメントテンプレート改訂の取り込み

## 1. 方針

- 反映元: `/home/sakagawa/git/DEV_TEMPLATE/template/`（コミット `715db11e1e9025a2818ce687a86552f76d25ab54`）。選別の根拠は本案件 `README.md` を参照
- 実施者: **Claude Code 本体**（転記・削除中心で Sonnet 委任のオーバーヘッドに見合わないため委任しない）
- テスト: **コード変更がないため自動テスト・手動テストとも不要**
- 変更対象ファイル: `CLAUDE.md`、`docs/BUGFIX_STANDARD.md`、`docs/REVIEW_CRITERIA.md`、`.gitignore`、（完了処理で）`docs/BACKLOG.md`・`docs/CHANGELOG.md`・本案件 `README.md`
- 変更しないファイル: `docs/DESIGN_STANDARD.md`・`docs/REQUIREMENTS_STANDARD.md`・`docs/codex-exec-ubuntu24-bwrap-fix.md`（テンプレートと完全一致）、`docs/TECH_STACK.md`・ルート `README.md`（記載内容に変更なし）、ソースコード・テストコード全部
- 変更方式の一覧: セクション2〜3・5は文字列置換・挿入（変更前→変更後を本書に全文記載）、セクション4のみ**ファイル全置換**（後処理なし。置換後にテンプレートとの完全一致を検証する）。セクション5には置換（追記）後の後処理として `git rm --cached` がある

以下、変更前・変更後のテキストはすべて完全一致で記載する。編集は上から順に実施してよい（相互依存なし）。

## 2. CLAUDE.md の変更（C1〜C9）

### C1: bug フロー ステップ1 の記録先明確化

「### 不具合修正フロー（bug-XXX 案件）」の手順1。

変更前:

```
1. **案件作成** → `docs/issues/bug-{number}-{slug}/` フォルダを作成し、`docs/BACKLOG.md` に追加する。`README.md` に不具合の概要と再現手順を記録する
```

変更後:

```
1. **案件作成** → `docs/issues/bug-{number}-{slug}/` フォルダを作成し、`docs/BACKLOG.md` に追加する。案件フォルダの `docs/issues/bug-{number}-{slug}/README.md` に不具合の概要と再現手順を記録する（ルートの `README.md` ではない）
```

### C2: bug フロー ステップ3 に requirements/design の併記

同節の手順3。

変更前:

```
3. **ドキュメント保存** → investigation.md の保存を確認する。**保存が完了するまで実装に進んではならない**
```

変更後:

```
3. **ドキュメント保存** → investigation.md の保存を確認する。調査の結果 `requirements.md` / `design.md` の修正が必要になった場合は、それらも併せて保存する。**保存が完了するまで実装に進んではならない**
```

### C3: 「ドキュメント更新フロー（update-XXX 案件）」節の新設

「### 不具合修正フロー（bug-XXX 案件）」の手順8の直後、「### ドキュメント作成ルール」見出しの直前に、以下を空行で挟んで挿入する。

```
### ドキュメント更新フロー（update-XXX 案件）

開発プロセスを定める運用ドキュメント（`CLAUDE.md`、`docs/` 直下の基準書・BACKLOG・CHANGELOG、`.gitignore` 等）の改訂は **update-XXX 案件**として扱い、以下のフローを**厳守**する。典型例:

- 本プロジェクトのコピー元テンプレートリポジトリ（開発ドキュメントテンプレート）の改訂の取り込み
- ドキュメント間の二重管理・不整合の解消、運用ルールの新設・変更

**ソースコード・テストコードの変更は含まない。** 作業中にコード変更が必要と判明した場合は中断し、feat/bug 案件として起票し直す。個別機能のドキュメント（案件フォルダ内の requirements.md 等）の修正は元案件側で扱い、update 案件にはしない。

要求仕様書・機能設計書は作らず、README.md（調査）と design.md（反映設計）の2点で代替する。

1. **案件作成** → `docs/issues/update-{number}-{slug}/` フォルダを作成し、`docs/BACKLOG.md` に追加する。slug は変更の目的がわかる名前にする（例: `adopt-dev-template`）
2. **調査** → 現状と変更理由を調査し、案件フォルダの `README.md` に記録する。テンプレート取り込みの場合は反映元パス・コミットID・差分の全量と、取り込む/取り込まない の選別と理由を書く。**この時点で反映先を編集してはならない**
3. **設計・保存** → 変更対象ファイルごとに「どのセクションを・どう変えるか」を `design.md` に書いてファイル保存する。自己完結（/clear 後でも design.md だけで反映作業ができる）・曖昧表現禁止。全置換に後処理が伴う場合は、変更方式の一覧・該当セクション・実施手順のすべてに明記する。完了処理（BACKLOG・CHANGELOG の更新）も設計に含める。**保存が完了するまで反映に進んではならない**
4. **レビュー（Codex → 人）** → 「Codexによるレビューの実行方法」に従う（バックグラウンド実行、`-o` + full.log 分離、`resume` による逐次再レビュー、重要度「高・中」ゼロ収束後に人レビュー）。レビュー対象は `README.md` と `design.md`。レビュー観点は次の3点を明示して依頼する:
   1. 反映計画の自己完結性（design.md だけで作業ができるか）
   2. 情報の喪失（削除・置換対象に、他所に存在しない情報が含まれていないか）
   3. 変更後のドキュメント間整合性（参照切れ、矛盾、案件の漏れ・重複）
5. **反映** → design.md に厳密に従って編集する。実装は Claude Code 本体が行ってよい（転記・削除中心で Sonnet 委任のオーバーヘッドに見合わないため。分量が大きい機械的変更では委任も可。どちらにするかは design.md に明記する）。設計にない変更が必要になったら中断してステップ2に戻る。反映後 `git diff` で「意図した変更のみか・保持対象が変わっていないか」を検証する
6. **完了** → `docs/BACKLOG.md` のステータスを Closed に更新する。`docs/CHANGELOG.md` に完了内容を記録し、案件 README のステータスを Closed に更新する。ファイルの追加・削除があった場合は `CLAUDE.md` のディレクトリ構成を最新に更新する
7. **テスト** → コード変更がないためテスト（自動・手動とも）は不要（不要であることを design.md に明記する）

#### 運用メモ

- 汎用性のある改善は、完了後にコピー元の開発ドキュメントテンプレートリポジトリへの還元（テンプレート側の update-XXX 案件）を検討する
- ルートの `.gitignore` がグローバル gitignore（`~/.gitignore_global`）の影響で未追跡になる環境では、コミットに含める際に `git add -f` が必要
```

### C4: 「ドキュメント作成ルール」の案件種別別再構成

「### ドキュメント作成ルール」の先頭4行を置換する。

変更前:

```
- **実装前に必ずドキュメントを作成し、案件フォルダにファイル保存すること**
- ドキュメントが保存されていない場合は、**実装を中止**する
- 機能追加時: 要求仕様書（`docs/REQUIREMENTS_STANDARD.md` 準拠）と機能設計書（`docs/DESIGN_STANDARD.md` 準拠）を作成する
- 不具合修正時: `docs/BUGFIX_STANDARD.md` の基準に従い、修正計画を `investigation.md` に記録する
```

変更後:

```
- **実装（反映）前に必ず案件種別に応じたドキュメントを作成し、案件フォルダにファイル保存すること**
  - feat: 要求仕様書（`requirements.md`、`docs/REQUIREMENTS_STANDARD.md` 準拠）と機能設計書（`design.md`、`docs/DESIGN_STANDARD.md` 準拠）
  - bug: 修正計画（`investigation.md`、`docs/BUGFIX_STANDARD.md` 準拠）。要求仕様書・機能設計書の変更が必要な場合はその変更案も併せて保存する
  - update: 調査記録（`README.md`）と反映設計書（`design.md`）。詳細は「ドキュメント更新フロー（update-XXX 案件）」に従う
- ドキュメントが保存されていない場合は、**実装を中止**する
```

同節内の以下の行も置換する。

変更前:

```
- ドキュメントは `docs/issues/{案件フォルダ}/` に置く（`requirements.md`, `design.md`, `investigation.md`）
```

変更後:

```
- ドキュメントは `docs/issues/{案件フォルダ}/` に置く。ファイル名は上記の案件種別ごとの必須ドキュメント定義に従う
```

### C5: 「案件ディレクトリ構成」への update 追記

コードブロック内のコメント行を置換する。

変更前:

```
└── {type}-{number}-{slug}/    # 例: bug-001-xxx, feat-001-yyy
```

変更後:

```
└── {type}-{number}-{slug}/    # 例: bug-001-xxx, feat-001-yyy, update-001-zzz
```

同節のコードブロック終了（` ``` `）の直後、「### 命名規則」見出しの直前に、以下を空行で挟んで挿入する。

```
update 案件は requirements.md / investigation.md を持たず、README.md（調査）・design.md（反映設計）・reviews/ で構成される。
```

### C6: Codex レビュー節冒頭の対象拡張

「### Codexによるレビューの実行方法」の冒頭段落を置換する。

変更前:

```
機能追加・不具合修正フローのステップ4（レビュー）では、Claude Code 自身が `codex exec` コマンドを実行して Codex にレビューさせる。Subagent は使わない。**Codex は逐次（前回セッションを `resume` で継続）で回し、重要度「高・中」がゼロに収束してから人レビューに進む**。並列にはしない（再レビューの収束確認＝「前回指摘が直ったか」の判定に前回文脈の引き継ぎが必要なため）。
```

変更後:

```
機能追加・不具合修正・ドキュメント更新フローのステップ4（レビュー）では、Claude Code 自身が `codex exec` コマンドを実行して Codex にレビューさせる。Subagent は使わない。**Codex は逐次（前回セッションを `resume` で継続）で回し、重要度「高・中」がゼロに収束してから人レビューに進む**。並列にはしない（再レビューの収束確認＝「前回指摘が直ったか」の判定に前回文脈の引き継ぎが必要なため。初回の発見網羅性を上げたい大規模案件でのみ「初回だけ多観点並列→以降逐次」を検討）。
```

### C7: 初回レビュー（不具合修正）コマンドの文言追加

「#### 初回レビュー（不具合修正の場合）」のコードブロック内の指示文字列を置換する。

変更前:

```
  "docs/REVIEW_CRITERIA.md および docs/BUGFIX_STANDARD.md の基準に従い、以下のドキュメントをレビューせよ: docs/issues/{案件フォルダ}/investigation.md 。瑣末な点へのクソリプはしないで、致命的な点のみ指摘して。発見した問題を重要度(高/中/低)で分類し、修正提案とともに報告すること。" \
```

変更後:

```
  "docs/REVIEW_CRITERIA.md および docs/BUGFIX_STANDARD.md の基準に従い、以下のドキュメントをレビューせよ: docs/issues/{案件フォルダ}/investigation.md 。requirements.md / design.md を変更した場合はそれらもレビュー対象に含めること。瑣末な点へのクソリプはしないで、致命的な点のみ指摘して。発見した問題を重要度(高/中/低)で分類し、修正提案とともに報告すること。" \
```

### C8: 「初回レビュー（ドキュメント更新の場合）」の新設

「#### 初回レビュー（不具合修正の場合）」のコードブロック終了直後、「#### 再レビュー（共通）」見出しの直前に、以下を空行で挟んで挿入する。

````
#### 初回レビュー（ドキュメント更新の場合）

```bash
mkdir -p docs/issues/{案件フォルダ}/reviews
codex exec -o docs/issues/{案件フォルダ}/reviews/codex-01.result.md \
  "以下のドキュメントをレビューせよ: docs/issues/{案件フォルダ}/README.md docs/issues/{案件フォルダ}/design.md 。レビュー観点は次の3点: (1) 反映計画の自己完結性（design.md だけで作業ができるか） (2) 情報の喪失（削除・置換対象に、他所に存在しない情報が含まれていないか） (3) 変更後のドキュメント間整合性（参照切れ、矛盾、案件の漏れ・重複）。瑣末な点へのクソリプはしないで、致命的な点のみ指摘して。発見した問題を重要度(高/中/低)で分類し、修正提案とともに報告すること。" \
  > docs/issues/{案件フォルダ}/reviews/codex-01.full.log 2>&1
```
````

### C9: Sonnet サブエージェント節の必読ドキュメント更新

「### 実装の実行方法（Sonnetサブエージェント）」の「#### サブエージェントへの指示に必ず含めること」の項目1を置換する。

変更前:

```
1. **必読ドキュメントと読む順序**: CLAUDE.md → 案件ドキュメント（機能追加は `requirements.md` と `design.md`、不具合修正は `investigation.md`）→ 変更対象コード → 参考にする既存テスト
```

変更後:

```
1. **必読ドキュメントと読む順序**: CLAUDE.md → 案件ドキュメント（機能追加は `requirements.md` と `design.md`、不具合修正は `investigation.md` と、変更した場合は関連する `requirements.md` / `design.md` も必読）→ 変更対象コード → 参考にする既存テスト
```

### CLAUDE.md で変更しない箇所（明示）

再レビュー節の `--last` の扱い（「使わない」を維持）、Sonnet/git サブエージェント節の確立日付注記、テスト・Bash 例・コーディング規約の具体化済み記述、命名規則の例。理由は `README.md` の選別表を参照。

## 3. docs/BUGFIX_STANDARD.md の変更（B1〜B3）

### B1: 1.1 不具合の特定に「エラーメッセージ」を追加

変更前:

```
- **現在の動作**: 何が起きているか（再現手順を含む）
- **期待する動作**: 要求仕様書・機能設計書の該当箇所を引用する
```

変更後:

```
- **現在の動作**: 何が起きているか（再現手順を含む）
- **期待する動作**: 要求仕様書・機能設計書の該当箇所を引用する
- **エラーメッセージ**: 完全なトレースバックを記録する
```

### B2: 1.3 修正内容に「修正コード」を追加

変更前:

```
- **変更しないファイル**: 影響範囲にあるが変更不要な場合、その理由
- **修正が設計書に沿っているか**: 設計書と矛盾する修正が必要な場合は明記する
```

変更後:

```
- **変更しないファイル**: 影響範囲にあるが変更不要な場合、その理由
- **修正コード**: 修正前・修正後のコードを示す
- **修正が設計書に沿っているか**: 設計書と矛盾する修正が必要な場合は明記する
```

### B3: 3 ステップ4〜5 のレビュー記述更新

変更前:

```
3. **ステップ4〜5（レビュー・修正）**: ユーザーとレビューを行い、問題があれば再調査する
```

変更後:

```
3. **ステップ4〜5（レビュー・修正）**: CLAUDE.md のレビュールールに従い、Codex の再帰レビューを重要度「高・中」ゼロまで回した後に人（ユーザー）レビューを行う。問題があれば再調査する
```

### 変更しない箇所（明示）

1.5「**自動テスト**: pytest で検証可能か（可能なら tests/ にテストを追加する）」は現状維持（テンプレートの汎用化を逆輸入しない）。

## 4. docs/REVIEW_CRITERIA.md の変更（R1: 全置換）

テンプレートの**コミット固定**の内容で `docs/REVIEW_CRITERIA.md` を**丸ごと置換**する（作業ツリーの状態に依存しない）:

```bash
git -C /home/sakagawa/git/DEV_TEMPLATE show 715db11e1e9025a2818ce687a86552f76d25ab54:template/docs/REVIEW_CRITERIA.md > docs/REVIEW_CRITERIA.md
```

- 根拠: 全差分（見出しレベル整理・観点の汎用化・1.10 重要度分類の新設）が採用対象であり、置換後はテンプレートと完全一致になる。差分の内訳と採用理由は `README.md` を参照
- 全置換に伴う後処理: なし（本リポジトリ固有の記述は現行ファイルに存在しない。失われる文言は「タイムアウト値」「接続失敗、タイムアウト」「Python版」「GUI」「FPS」等の具体例のみで、いずれもテンプレート版の汎用表現に包含される）
- 検証: 置換後に `git -C /home/sakagawa/git/DEV_TEMPLATE show 715db11e1e9025a2818ce687a86552f76d25ab54:template/docs/REVIEW_CRITERIA.md | diff - docs/REVIEW_CRITERIA.md` が差分なしであることを確認する

## 5. .gitignore の変更（G1）

### G1: Claude Code ローカルファイルの ignore 追加と追跡解除

ファイル末尾（現行最終行「`docs/issues/*/experiments/*/data/`」の後）に、空行を挟んで以下を追記する。

```
# Claude Code のローカル設定・引き継ぎノート
.claude/settings.local.json
.claude/handovers/
```

後処理（追記後に実行）:

```bash
git rm --cached .claude/settings.local.json
git rm -r --cached .claude/handovers
```

- ワークツリーのファイルは残る（`--cached`）。リポジトリからは次回コミットで削除される（過去分は git 履歴から参照可能なため情報喪失はない）
- `.claude/handovers/` は 2026-07-02 以前の14件が追跡済み（`git ls-files .claude/handovers` で確認済み）。それ以降の未追跡分は ignore 追加のみで足りる
- handovers 14件は棚卸し済み（README.md「追跡済み handovers 14件の棚卸し」参照）。恒久化すべき情報は案件ドキュメント・CHANGELOG・git 履歴・Claude Code メモリに収載済みで、**docs への転記対象はなし**と決定済み。唯一未収載だった教訓（引き継ぎノートの実在確認）はメモリ `feedback_handover_verify_disk_state` に保存済み（2026-08-03、実施済みのため反映ステップでの追加作業はない）
- `.claude/settings.json`（プロジェクト共有設定）が存在する場合は対象外（追跡を維持）

## 6. 完了処理（反映・検証後に実施）

1. `docs/BACKLOG.md`: update-001 の行のステータスを `Open` → `Closed` に変更する
2. `docs/CHANGELOG.md`: 「## リリース履歴」直下（`### 2026-08-02` の上）に以下を挿入する（日付は完了日。以下は 2026-08-03 完了の場合）:

   ```
   ### 2026-08-03

   - **update-001**: 開発ドキュメントテンプレート改訂の取り込み（update-XXXフロー導入）
     - CLAUDE.md に「ドキュメント更新フロー（update-XXX 案件）」を新設（README.md 調査 + design.md 反映設計の2点構成、Codex 3観点レビュー、テスト不要）。ドキュメント作成ルール・案件ディレクトリ構成・Codex レビュー節を案件種別 update に対応
     - bug フローの明確化（記録先は案件フォルダの README.md、requirements/design 変更時の保存・レビュー対象化）
     - BUGFIX_STANDARD.md: エラーメッセージ（完全なトレースバック）・修正コード（前後）の記録を必須項目に追加、ステップ4〜5 を Codex 再帰レビュー運用に整合
     - REVIEW_CRITERIA.md: テンプレート版で全置換（観点の汎用化、ドキュメントレビューの重要度分類 1.10 を新設）
     - .gitignore: `.claude/settings.local.json` と `.claude/handovers/` を ignore に追加し、追跡済み分（settings.local.json 1件 + handovers 14件）を追跡解除（ワークツリー・git 履歴には残存）
     - 反映元: DEV_TEMPLATE/template コミット 715db11。取り込み選別・Codex レビューは `docs/issues/update-001-adopt-dev-template/`
   ```

3. 本案件 `README.md`: ステータスを `Open（2026-08-03 起票）` → `Closed（2026-08-03 起票、YYYY-MM-DD 完了）` に更新する
4. `CLAUDE.md` のディレクトリ構成: 変更不要（追加ファイルは案件フォルダ内のみで、ディレクトリ構成には個別案件フォルダを列挙していない）
5. ルート `README.md`: 変更不要（コマンド・CLIオプション・入出力形式に変更なし）

## 7. 反映後の検証

- `git diff` で変更が本書記載の範囲のみであることを確認する（意図しない削除・改変がないこと）
- セクション4の検証コマンドで `docs/REVIEW_CRITERIA.md` がテンプレート（コミット `715db11`）と完全一致することを確認する
- `git status` で `.claude/settings.local.json`（1件）と `.claude/handovers/` 配下の14件がステージ済み削除（D）になり、M 表示が消えることを確認する
- `git check-ignore .claude/settings.local.json .claude/handovers/` が両パスを ignore 対象と報告することを確認する
- `ls .claude/handovers/ | wc -l` 等でワークツリーのファイルが削除されていない（追跡解除のみ）ことを確認する

## 改訂履歴

- 2026-08-03: 初版（Codex レビュー前）
- 2026-08-03: codex-01 指摘対応（高: G1 に追跡済み handovers 14件の追跡解除を追加、高: C9 必読ドキュメント更新を追加、中: REVIEW_CRITERIA 全置換をコミット固定の `git show` 取得に変更）
- 2026-08-03: codex-02 指摘対応（中: 追跡済み handovers 14件の棚卸しを実施し README に明記。転記対象なし、唯一未収載の教訓はメモリに保存済み）
