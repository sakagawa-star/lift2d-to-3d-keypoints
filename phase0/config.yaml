# Phase0 検証用設定ファイル

# 検証対象のカメラ名
target_camera: cam01

# カメラパラメータCSV（全カメラ分）
camera_params: camera_params.csv

# 基準点3D座標CSV
points_3d: kijunten_locations.csv

# 基準点2D座標CSV（全カメラ分、縦持ち）
points_2d: points_2d.csv
